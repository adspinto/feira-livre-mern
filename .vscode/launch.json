{
    "version": "0.2.0",
    "configurations": [      
      {
        "command": " env $(cat .env | grep -v '#' | xargs) yarn dev",
        "name": "Run yarn start",
        "request": "launch",
        "type": "node-terminal"
      }
    ]
  }